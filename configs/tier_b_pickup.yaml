experiment_id: tier_b_pickup_cond_a
# Not runnable until the Phase 2 Allegro environment is implemented.
env_id: DynHand-AllegroPickup-v0
seed: 0
condition: A
total_env_steps: 2000000
num_envs: 12
start_steps: 10000
checkpoint_interval: 50000

# Phase 2 must register this environment before this config is usable.
